var element = document.querySelector(`[data-seos-id="<%= dom_id(@seo) %>"]`);
if (element) {
  element.style.transition = "opacity 0.2s ease-out"; // fast equivalent
  element.style.opacity = 0;

  setTimeout(function() {
    element.remove();
  }, 200); // match "fast" timing
}
