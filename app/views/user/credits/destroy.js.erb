var el = document.getElementById("credit_<%= @credit.id %>");
if (el) {
  el.style.transition = "all 0.4s ease";
  el.style.opacity = "0";
  el.style.transform = "scale(0.95)";
  setTimeout(() => el.remove(), 400);
}
